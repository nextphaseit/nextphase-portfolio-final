export default function HomePage() {
  return (
    <div className="min-h-screen bg-white">
      {/* All existing homepage content */}

      {/* Chatbot is now in layout, so remove from here if present */}
    </div>
  )
}
